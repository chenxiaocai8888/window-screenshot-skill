interface:
  display_name: "Window Screenshot"
  short_description: "Capture exact Windows screenshots."
  default_prompt: "Use $window-screenshot to capture and verify the requested screenshot."
policy:
  allow_implicit_invocation: true
